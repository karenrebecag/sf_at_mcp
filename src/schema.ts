/** Re-export for backward compatibility — canonical definitions live in schema/resources.ts */
export {
  SCHEMA_MD,
  SCHEMA_RESOURCE_URI,
  SCHEMA_RESOURCES,
  SCHEMA_SECTION_URIS,
} from './schema/resources.js';
